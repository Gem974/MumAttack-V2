using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Plaice : MonoBehaviour
{

    private void OnTriggerEnter(Collider other)
    {
        //if (other.gameObject.tag == "Player")
        //{
        //    GameManager_IromMum.instance.AddPoints(other.GetComponent<Kart>()._isPlayer1);
        //    //Debug.Log("Points: " + GameManager.instance._points);
        //    Destroy(gameObject);
        //}
    }
}
